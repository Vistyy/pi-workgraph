{
  "name": "@vistyy/pi-workgraph",
  "version": "0.1.0",
  "packageManager": "pnpm@11.25.0",
  "type": "module",
  "description": "Conversational, durable local work coordination for Pi.",
  "engines": {
    "node": ">=24"
  },
  "license": "MIT",
  "bin": {
    "pi-workgraph": "./bin/pi-workgraph.mjs"
  },
  "keywords": [
    "pi-package",
    "pi",
    "orchestration",
    "worktree"
  ],
  "files": [
    "bin",
    "extensions",
    "src",
    "skills",
    "README.md",
    "DESIGN.md",
    "VERIFICATION.md"
  ],
  "scripts": {
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "quality:prepare": "effect-tsgo patch --oxlint --no-typescript",
    "quality": "pnpm run quality:prepare && biome check --error-on-warnings . && oxlint --config oxlint.config.ts .",
    "check": "pnpm run quality && pnpm run test",
    "smoke:herdr": "node --import tsx scripts/live/native-smoke.ts",
    "smoke:coordinator": "node --import tsx scripts/live/capability-smoke.ts",
    "smoke:natural": "node --import tsx scripts/live/natural-smoke.ts"
  },
  "pi": {
    "extensions": [
      "./extensions/coordinator.ts",
      "./extensions/worker.ts"
    ],
    "skills": [
      "./skills"
    ]
  },
  "dependencies": {
    "@effect/platform-node-shared": "4.0.0-rc.112",
    "effect": "4.0.0-rc.112",
    "tsx": "^4.23.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@earendil-works/pi-ai": "0.84.4",
    "@earendil-works/pi-coding-agent": "0.84.4",
    "@earendil-works/pi-tui": "0.84.4",
    "@effect/tsgo": "0.41.0",
    "@syzom/typescript-quality": "0.2.0",
    "@types/node": "^24.0.0",
    "oxlint": "1.81.0",
    "oxlint-tsgolint": "7.0.2001",
    "typebox": "^1.0.0",
    "typescript": "7.0.2"
  }
}
